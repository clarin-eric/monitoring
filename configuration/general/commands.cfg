###
##  general
###

define command {
    command_name    check_dummy
    command_line    $USER1$/check_dummy 0
}

define command {
    command_name	check_http_redefined
    command_line	$USER1$/check_http $ARG1$
}

define command {
    command_name    freshness_warning
    command_line    $USER1$/check_dummy 4 "UNKNOWN: Attention, did not get fresh checks!"
    # TODO: meaning?
}

define command {
    command_name    check-ping
    command_line    $USER1$/check_ping -H $HOSTADDRESS$ -w 3000.0,80% -c 5000.0,100% -p 5
}

define command {
    command_name    curl_probe
    command_line    curl --tlsv1.2 -e 'https://clarin.fz-juelich.de/icinga/' -f -L -s -v -o /dev/null -w '@$USER2$/curl.format' '$ARG1$://$ARG2$:$ARG4$/$ARG3$' 2>&1 || exit 2
}

define command {
    command_name    clarin_saml_probe
    command_line    $USER2$/check_clarin_saml.sh '$HOSTADDRESS$' 2>&1
}

define command {
    command_name    clarin_ldap_probe
    command_line    sudo $USER2$/check_clarin_ldap.py -h '$HOSTADDRESS$' -b 'cn=Directory Manager' 2>&1
}

define command {
    command_name    clarin_central_discovery_service_json_probe
    command_line    $USER2$/check_json.sh '$HOSTADDRESS$'' '/metadata/clarin_spf_idps.json' 2>&1
    # TODO: parameterize
}

###
##  WebLicht
###

define command {
    command_name    jar_wrapper
    command_line    $USER2$/tue/jar_wrapper.sh $ARG1$ $ARG2$ $ARG3$
}

###
##  Tübingen
###

define command {
    command_name    check_convert-all
    command_line    $USER2$/tue/check_convert-all.sh $HOSTADDRESS$
}