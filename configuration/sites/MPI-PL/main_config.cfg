define hostgroup {
    alias Max Planck Institute Nijmegen
    hostgroup_name MPI-PL-hostgroup
}

define host {
	contacts latadmin@mpi.nl
    hostgroups MPI-PL-hostgroup
    name MPI-PL-host
    register 0
    use custom-active-host
}

define host {
    alias TLA applications [prod]
    host_name corpus1.mpi.nl
    use MPI-PL-host
}

define host {
    host_name cqlservlet.mpi.nl
    use MPI-PL-host
}

define host {
    host_name catalog.clarin.eu
    use MPI-PL-host
}

define host {
    alias CLARIN IAM [prod]
    host_name user.clarin.eu
    use MPI-PL-host
}

define host {
    alias CLARIN infra [prod]
    host_name infra.clarin.eu
    use MPI-PL-host
}


define host {
    alias CLARIN SVN [prod]
    host_name svn.clarin.eu
    use MPI-PL-host
}

define host {
    host_name discovery.clarin.eu
    use MPI-PL-host
}
