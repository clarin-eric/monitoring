define servicegroup {
     servicegroup_name       MPI-PL-servicegroup
}

# Template
define service {
    check_interval           60
    name                     MPI-PL-service
    register                 0
    servicegroups            MPI-PL-servicegroup
    use                      clarin-direct-warning
}

define service {
    check_command            probe_ldap_bind!ldap!user.clarin.eu!ou=members,dc=clarin,dc=eu!10389
    host_name                user.clarin.eu
    service_description      CLARIN LDAP directory [prod]
    use                      MPI-PL-service
}

# TODO: remove last unnecessary special case commands

define service {
    check_command            probe_handle_json!https!hdl.handle.net!api/handles/1839/00-0000-0000-0000-0000-4?noredirect&auth&cert&pretty!443!--tlsv1
    display_name             Handle status as JSON
    host_name                MPI-PL
    service_description      handle-resolving
    use                      srv-pnp,MPI-PL-service
}

define service {
    check_command            probe_imdi!https!hdl.handle.net!1839/00-0000-0000-0000-0000-4?auth!443!--tlsv1
    display_name             Handle resolve to IMDI
    host_name                MPI-PL
    service_description      handle-doc-access
    use                      srv-pnp,MPI-PL-service
}

define service {
    check_command            probe_http_head!https!$HOSTADDRESS$!ds/annex/runLoader?nodeid=MPI600401%23!443!!
    host_name                corpus1.mpi.nl
    service_description      Annex
    use                      MPI-PL-service
}

define service {
    check_command            probe_http_head!https!$HOSTADDRESS$!jkc/lamus/lamusstart.jsp!443!!
    host_name                corpus1.mpi.nl
    service_description      Lamus
    use                      MPI-PL-service
}

define service {
    check_command            probe_http_head!https!$HOSTADDRESS$!lex/lexus/!443!!
    host_name                corpus1.mpi.nl
    service_description      Lexus
    use                      MPI-PL-service
}

define service {
    check_command            probe_http_head!https!$HOSTADDRESS$!ds/ComponentRegistry/!443!!
    host_name                catalog.clarin.eu
    service_description      CLARIN Component Registry [UI][prod]
    use                      MPI-PL-service
}

define service {
    check_command            probe_http_head!https!$HOSTADDRESS$!vlo/!443!!
    host_name                catalog.clarin.eu
    service_description      CLARIN VLO catalog [UI][prod]
    use                      MPI-PL-service
}

define service {
    check_command            probe_saml_metadata!https!$HOSTADDRESS$!aai/prod_md_about_spf_idps.xml!443
    host_name                infra.clarin.eu
    service_description      CLARIN SPF prod_md_about_spf_idps
    use                      MPI-PL-service
}

define service {
    check_command            probe_saml_metadata!https!$HOSTADDRESS$!aai/prod_md_about_clarin_erics_idp.xml!443
    host_name                infra.clarin.eu
    service_description      CLARIN SPF prod_md_about_clarin_erics_idp
    use                      MPI-PL-service
}

define service {
    check_command            probe_http_head!https!$HOSTADDRESS$!!443!401!
    host_name                svn.clarin.eu
    service_description      CLARIN SVN
    use                      MPI-PL-service
}

define service {
    check_command            probe_http_head!https!$HOSTADDRESS$!!443!401!
    host_name                trac.clarin.eu
    service_description      CLARIN Trac
    use                      MPI-PL-service
}