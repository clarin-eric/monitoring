# Template

# TODO: remove last unnecessary special case commands
define service {
    check_interval           60
    contacts                 gAAAAABVZx5t172NoG9e_tq-fF19MvWk8Q7mb9VJrNw-8Mi8l5yTNJnwGrLm_GNGi2OFCQEq-QoCzrfOq0ljDA73UvByCB55JTGRsk5Jsm5tgtJzCk6o5Fw=,gAAAAABVZx5sgoTDhM8m6twtI6pev7AhBYETVU4-7Ao2paMX4qhP9vHMr84goO9Jye5-RL0CBzmUZCjwHZg2bi3UrFNTMupvxA==
    name                     MPI-PL-service
    register                 0
    servicegroups            MPI-PL
    use                      clarin-direct-warning
}

define service {
    check_command            check_http_redefined! -H "hdl.handle.net" -u "/1839/00-0000-0000-0000-0000-4?noredirect" -t 20
    display_name             "Handle resolving /1839/00-0000-0000-0000-0000-4?noredirec"
    host_name                MPI-PL
    service_description      handle-resolving
    use                      srv-pnp,MPI-PL-service
}

define service {
    check_command            check_http_redefined! -H "hdl.handle.net" -u "/1839/00-0000-0000-0000-0000-4" -t 20 -f follow
    display_name             "Handle document access /1839/00-0000-0000-0000-0000-4"
    host_name                MPI-PL
    service_description      handle-doc-access
    use                      srv-pnp,MPI-PL-service
}

define service {
    check_command            check_annex!https!/ds/annex/runLoader?nodeid=MPI600401%23!443
    host_name                corpus1.mpi.nl
    service_description      Annex
    use                      custom-active-service,MPI-PL-service
}

define service {
    check_command            check_handle!http!hdl.handle.net!/?hdl=1839%2F00-0000-0000-0000-0000-4&auth=x!80
    host_name                corpus1.mpi.nl
    service_description      Handle
    use                      custom-active-service,MPI-PL-service
}

define service {
    check_command            check_lamus!https!/jkc/lamus/lamusstart.jsp!443
    host_name                corpus1.mpi.nl
    service_description      Lamus
    use                      custom-active-service,MPI-PL-service
}

define service {
    check_command            check_lexus!https!/lex/lexus/!443
    host_name                corpus1.mpi.nl
    service_description      Lexus
    use                      custom-active-service,MPI-PL-service
}

# TODO: no need for this generic check?
# define service {
#    check_command            check_http
#    host_name                corpus1.mpi.nl
#    service_description      HTTP
#    use                      custom-active-service,MPI-PL-service
# }

define service {
    check_command            curl_probe!https!$HOSTADDRESS$!/ds/ComponentRegistry/!443
    host_name                catalog.clarin.eu
    service_description      CLARIN Cmp. Rg. [UI][prod]
    use                      custom-active-service,MPI-PL-service
}

define service {
    check_command            curl_probe!https!$HOSTADDRESS$!/vlo/!443
    host_name                catalog.clarin.eu
    service_description      CLARIN VLO catalog [UI][prod]
    use                      custom-active-service,MPI-PL-service
}

define service {
    check_command            curl_probe!https!$HOSTADDRESS$!/!443
    service_description      CLARIN VLO HostEurope [UI][prod]
    host_name                vlo.clarin.eu
    use                      custom-active-service,MPI-PL-service
}

define service {
    check_command            curl_probe!https!$HOSTADDRESS$!/profile/Status!443
    host_name                idp.clarin.eu
    service_description      CLARIN IdP [UI][prod]
    use                      MPI-PL-service
}

define service {
    check_command            curl_probe!https!$HOSTADDRESS$!/!443
    host_name                www.clarin.eu
    service_description      Official CLARIN website [prod]
    use                      custom-active-service,MPI-PL-service
}

define service {
    check_command            curl_probe!https!$HOSTADDRESS$!/discojuice!443
    host_name                discovery.clarin.eu
    service_description      CLARIN DS [UI][prod]
    use                      custom-active-service,MPI-PL-service
}

define service {
    check_command            curl_probe_json!https!$HOSTADDRESS$!/metadata/clarin_spf_idps.json!443
    host_name                discovery.clarin.eu
    service_description      CLARIN DS [JSON][prod]
    use                      custom-active-service,MPI-PL-service
}

define service {
    check_command            curl_probe!http!$HOSTADDRESS$!/vcr!443
   host_name                 clarin.ids-mannheim.de
    service_description      CLARIN VCR [UI][prod]
    use                      custom-active-service,MPI-PL-service
}

define service {
    check_command            curl_probe!https!$HOSTADDRESS$!/about!443
    host_name                centres.clarin.eu
    service_description      CLARIN Cntr. Rg. [UI][prod]
    use                      custom-active-service,MPI-PL-service
}

## TODO: add more SPF metadata checks
#define service {
#    check_command            curl_probe_xml!https!$HOSTADDRESS$!/aai/prod_md_about_spf_idps.xml!443
#    host_name                infra.clarin.eu
#    service_description      CLARIN SPF mtdt. [prod]
#    use                      custom-active-service,MPI-PL-service
#}
#
# TODO:
# define service {
#    check_command            check_clarin_ldap
#    host_name                user.clarin.eu
#    service_description      CLARIN LDAP directory [prod]
#    use                      custom-active-service,MPI-PL-service
# }