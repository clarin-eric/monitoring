define service {
        host_name               HZSK
        use                     custom-active-service,srv-pnp
        check_interval          60
        service_description     Clarin-D Helpdesk access
        check_command           check_http_redefined! -H support.clarin-d.de -u /otrs/index.pl -s "Welcome to CLARIN-D Helpdesk"
        servicegroups           HZSK
	contacts                      dummy
}
