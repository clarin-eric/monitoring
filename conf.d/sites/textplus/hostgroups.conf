##
# Each HostGroup needs a UserGroup with the same name and vice versa due to
# CLARIN monitoring's /conf.d/notifiations.conf!
##

object HostGroup "TEXTPLUS-ORG-HOSTS" {
    display_name = "TEXTPLUS-ORG"
    assign where host.vars.textplusorg == true
}

