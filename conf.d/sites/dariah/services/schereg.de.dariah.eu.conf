# Schema Registry (produktiv)
object Service "Schema Registry Frontend" {
  import "generic-service"
  host_name = "schereg.de.dariah.eu"
  check_command = "http"
  vars.http_ssl = false
  vars.http_vhost = "schereg.de.dariah.eu"
  vars.http_uri = "/schereg/registry"
  vars.http_onredirect = "follow"
  vars.http_string = "DARIAH-DE Data Modeling Environment"
}

