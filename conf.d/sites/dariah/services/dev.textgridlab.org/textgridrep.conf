object Service "DEV TextGrid Repository Webseite" {
  import "generic-service"
  host_name = "dev.textgridrep.org"
  check_command = "http"
  vars.http_ssl = true
  vars.http_vhost = "dev.textgridrep.org"
  vars.http_uri = ""
  vars.http_onredirect = "follow"
  vars.http_string = "TextGrid Repository"
}

