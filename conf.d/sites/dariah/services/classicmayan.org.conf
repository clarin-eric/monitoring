object Service "RDF Mask HTML Index Page" {
  import "generic-service"
  host_name = "classicmayan.org"
  check_command = "http"
  vars.http_ssl = true
  vars.http_vhost = "classicmayan.org" 
  vars.http_uri = "/rdfmask/"
  vars.http_onredirect = "follow"
  vars.http_string = "Input Mask of Classical Mayan Object Description"
  
}

object Service "Fuseki HTML Index Page" {
  import "generic-service"
  host_name = "classicmayan.org"
  check_command = "http"
  vars.http_ssl = true
  vars.http_vhost = "classicmayan.org" 
  vars.http_uri = "/trip/"
  vars.http_string = "Apache Jena Fuseki"
  
}

object Service "IDIOM Schema" {
  import "generic-service"
  host_name = "classicmayan.org"
  check_command = "http"
  vars.http_ssl = true
  vars.http_vhost = "classicmayan.org" 
  vars.http_uri = "/idiommask/schema.html"
  vars.http_string = "IDIOM Schema"
  
}

object Service "IDIOM-Sign Catalog" {
  import "generic-service"
  host_name = "classicmayan.org"
  check_command = "http"
  vars.http_ssl = true
  vars.http_vhost = "classicmayan.org" 
  vars.http_uri = "/catalogue"
  vars.http_string = "Ontology of the Sign Catalogue for Classic Mayan"
  
}

