object Service "TEST TextGrid Repository Webseite" {
  import "generic-service"
  host_name = "test.textgridrep.org"
  check_command = "http"
  vars.http_ssl = true
  vars.http_vhost = "test.textgridrep.org"
  vars.http_uri = ""
  vars.http_onredirect = "follow"
  vars.http_string = "TextGrid Repository"
}

